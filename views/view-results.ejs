<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Test Results</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .results-container {
            background-color: #f8f9fa;
            border-radius: 5px;
            padding: 20px;
            white-space: pre-wrap;
            font-family: monospace;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <h2>Test Results: <%= booking.test_name %></h2>
        <p>Patient: <%= booking.patient_name %></p>
        <p>Date: <%= new Date(booking.results_date).toLocaleString() %></p>
        
        <div class="results-container">
            <%= booking.results %>
        </div>
        
        <div class="mt-3">
            <a href="/bookings/<%= booking.id %>" class="btn btn-primary">Back to Booking</a>
            <a href="/bookings" class="btn btn-secondary">All Bookings</a>
        </div>
    </div>
</body>
</html>