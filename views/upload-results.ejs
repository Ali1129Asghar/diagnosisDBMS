<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Upload Test Results</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h2>Upload Results for <%= booking.test_name %></h2>
        <p>Patient: <%= booking.patient_name %></p>
        
        <form action="/bookings/<%= booking.id %>/results" method="POST">
            <div class="mb-3">
                <label class="form-label">Test Results</label>
                <textarea name="results" class="form-control" rows="10" required></textarea>
            </div>
            
            <button type="submit" class="btn btn-primary">Upload Results</button>
            <a href="/bookings/<%= booking.id %>" class="btn btn-secondary">Cancel</a>
        </form>
    </div>
</body>
</html>